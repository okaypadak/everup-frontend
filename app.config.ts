export default defineAppConfig({
  ui: {
    primary: 'primary',
    gray: 'neutral',
    radius: 'md',
    font: {
      sans: 'var(--font-sans)',
      mono: 'var(--font-mono, monospace)'
    }
  }
})
